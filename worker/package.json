{
  "name": "matchday-ai-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Holds the shared Gemini key so Matchday players don't each need their own",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setup": "bash setup.sh",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.99.0"
  }
}
